import { Box } from '@mui/material'
import { commonStyles } from 'components/common.styles'

const Contacts = () => (
  <Box sx={commonStyles.pageContent}>Contacts go here.</Box>
)

export default Contacts
